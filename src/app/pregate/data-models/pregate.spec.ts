/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Pregate} from './pregate';

describe('Pregate', () => {
  it('should create an instance', () => {
    expect(new Pregate()).toBeTruthy();
  });
});
