/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {FacilityRedirection} from './facility-redirection';

describe('FacilityRedirection', () => {
  it('should create an instance', () => {
    expect(new FacilityRedirection()).toBeTruthy();
  });
});
