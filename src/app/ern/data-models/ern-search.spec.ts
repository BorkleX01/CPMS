/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {ERNSearch} from './ern-search';

describe('ERNSearch', () => {
  it('should create an instance', () => {
    expect(new ERNSearch()).toBeTruthy();
  });
});
